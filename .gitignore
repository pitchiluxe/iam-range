node_modules/
dist/
.vite/
coverage/
*.log

# Packaging output. The installer is a release asset, not a source file.
dist-installer/

# Generated by `npm run build:icons` from build-icons.cjs, which is the source.
build/
public/favicon.svg

# site/favicon.svg is NOT ignored, even though it is generated. Vercel serves
# site/ with no build step, so anything the page references has to be in the
# repository or it 404s on the deployed site.
